#include "Point.hpp"

Point::Point(int x, int y){ 
	this->x=x;
	this->y=y;
	}

// Fonction pour calculer la distance euclidienne entre deux points
double::Point distanceEuclidienne(Point& cible) {
    return sqrt(pow((this->x - cible.x, 2) + pow(this->y - cible.y, 2));
}




